package grape

import (
	"testing"
)

func TestScanner(t *testing.T) {
	/*s := &scanner{src: "\t\t\tAbc (yes)\ndiv.a"}
	
	if tk := s.scan(); tk != tkIndent {
		t.Fatal("expected tkIndent, got ", tk)
	}
	if data := s.data(); data != "\t\t\t" {
		t.Fatalf(`expected "\t\t\t", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkSelector {
		t.Fatal("expected tkSelector, got ", tk)
	}
	if data := s.data(); data != "Abc" {
		t.Fatalf(`expected "abc", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkLeftBracket {
		t.Fatal("expected tkLeftBracket, got ", tk)
	}
	if data := s.data(); data != "(" {
		t.Fatalf(`expected "(", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkIdentifier {
		t.Fatal("expected tkIdentifier, got ", tk)
	}
	if data := s.data(); data != "yes" {
		t.Fatalf(`expected "yes", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkRightBracket {
		t.Fatal("expected tkRightBracket, got ", tk)
	}
	if data := s.data(); data != ")" {
		t.Fatalf(`expected ")", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkLineBreak {
		t.Fatal("expected tkLineBreak, got ", tk)
	}
	if data := s.data(); data != "\n" {
		t.Fatalf(`expected "\n", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkSelector {
		t.Fatal("expected tkSelector, got ", tk)
	}
	if data := s.data(); data != "div.a" {
		t.Fatalf(`expected "div.a", got %q`, data)
	}
	
	if tk := s.scan(); tk != tkEOF {
		t.Fatal("expected tkEOF, got ", tk)
	}*/
}